enum WeaponCategory {
  Access, Watch, Reserve,
}

enum ResistanceMechanism {
  None, BetaLactamase, TargetModification, EffluxPump, TissueBarrier,
}

enum SupportAction {
  Inspection, SourceControl, // 検査, 感染源制御
}